// ── Página: Manual de Marca (diretoria) ──
import { shell } from '/assets/js/ui/shell.js';

await shell.mount({
  activeRoute: '/membros/diretoria/manual-de-marca',
  pageTitle: 'Manual de Marca',
});
